<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.PresentationViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        mc:Ignorable="d"
        Title="Pool" Height="600" Width="1000">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Button Content="+" Command="{Binding AddBallCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="280,30,0,0" Width="20" Height="20" />
        <Button Content="-" Command="{Binding RemoveBallCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="80,30,0,0" Width="20" Height="20"/>
        <TextBlock Text="{Binding Balls.Count, StringFormat={}Liczba kuli: {0}}" TextAlignment="Center" VerticalAlignment="Center" FontSize="22" HorizontalAlignment="Left" Margin="100,21,0,507" Width="180"/>
        <Border BorderThickness="10" Height="400" Width="800" BorderBrush="Brown" >
            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Name="CanvasName" Background="Green" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width= "{Binding Diameter}" Height="{Binding Diameter}" Fill="White" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </Grid>
</Window>